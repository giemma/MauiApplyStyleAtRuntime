<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             
             xmlns:viewModels="clr-namespace:MauiApplyStyleAtRuntime.ViewModels" 
             
             x:Class="MauiApplyStyleAtRuntime.MainPage">

    <ContentPage.BindingContext>
        <viewModels:MainPageViewModel />
    </ContentPage.BindingContext>
    
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <StackLayout Grid.Row="1" Orientation="Horizontal" >
                <Button Command="{Binding SetTheme1}" Text="Theme1" Margin="10"></Button>
                <Button Command="{Binding SetTheme2}" Text="Theme2" Margin="10"></Button>
                <Button Command="{Binding SetTheme3}" Text="Theme3" Margin="10"></Button>
                <Button Command="{Binding SetTheme4}" Text="Theme4" Margin="10"></Button>
            </StackLayout>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
